import {React, Header, ListView, View, styles} from './imports';

const ProfessionScreen = () => {
  return (
    <View style={styles.root}>
      <Header />
      <ListView />
    </View>
  );
};

export default ProfessionScreen;
