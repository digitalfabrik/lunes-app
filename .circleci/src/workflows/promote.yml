when: << pipeline.parameters.run_promote >>
jobs:
  - promote_android:
      context:
        - mattermost
        - tuerantuer-google-play
  - promote_ios:
      context:
        - mattermost
        - tuerantuer-apple
  - move_release_notes:
      context:
        - mattermost
        - deliverino
      requires:
        - promote_android
        - promote_ios
  - notify_promotion:
      context:
        - mattermost
        - deliverino
      requires:
        - move_release_notes
